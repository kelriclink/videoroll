"""Workflow-engine migration boundary for VideoRoll.

Keep this package import-light: the dedicated Hatchet worker image intentionally
has a smaller dependency set than the main VideoRoll application image.
"""
