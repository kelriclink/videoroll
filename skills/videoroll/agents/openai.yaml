interface:
  display_name: "VideoRoll"
  short_description: "Collect YouTube URLs and push them to VideoRoll"
  default_prompt: "Use $videoroll to collect YouTube watch URLs on an external machine and submit them to VideoRoll through the remote API."
